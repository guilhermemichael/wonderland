import { ClientRabbitExperience } from "./ClientRabbitExperience";

export default function RabbitEntryPage() {
  return (
    <main className="landing rabbit-entry" aria-labelledby="rabbit-title">
      <ClientRabbitExperience />
    </main>
  );
}
