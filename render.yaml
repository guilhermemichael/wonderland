services:
  - type: web
    name: wonderland-api
    env: python
    region: virginia
    plan: free
    buildCommand: "pip install -r apps/api/requirements.txt"
    startCommand: "cd apps/api && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: "/api/v1/health"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: wonderland-postgres
          property: connectionString
      - key: API_CORS_ORIGINS
        value: "https://wonderland-ochre.vercel.app,http://localhost:3000,http://127.0.0.1:3000"
      - key: APP_ENV
        value: "production"

databases:
  - name: wonderland-postgres
    databaseName: wonderland_postgres
    user: wonderland_postgres_user
    region: virginia
    plan: free
    postgresMajorVersion: "16"
